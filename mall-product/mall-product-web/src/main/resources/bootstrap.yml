server:
  port: 18082
  servlet:
    context-path: /

spring:
  profiles:
    # 环境 dev|test|prod
    active: dev
  application:
    name: mall-product
  cloud:
    nacos:
      discovery:
        #naocs-server地址
        server-addr: http://192.168.230.7:18848
      config:
        # 配置中心地址
        server-addr: http://192.168.230.7:18848
  config:
    import:
      - optional:nacos:mall-product-${spring.profiles.active}.yaml
      - optional:nacos:mall-redis-${spring.profiles.active}.yaml
