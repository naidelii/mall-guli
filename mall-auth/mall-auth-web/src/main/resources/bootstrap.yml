server:
  port: 18081
  servlet:
    context-path: /

spring:
  profiles:
    # 环境 dev|test|prod
    active: dev
  application:
    name: mall-auth
  cloud:
    nacos:
      discovery:
        #naocs-server地址
        server-addr: http://192.168.230.7:18848
      config:
        # 配置中心地址
        server-addr: http://192.168.230.7:18848
  config:
    import:
      - optional:nacos:mall-auth-${spring.profiles.active}.yaml
      - optional:nacos:mall-redis-${spring.profiles.active}.yaml